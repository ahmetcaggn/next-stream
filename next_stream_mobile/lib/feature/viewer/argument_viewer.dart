/// The argument for the viewer screen.
class ArgumentViewer {
  /// Creates the argument for the viewer screen.
  ArgumentViewer({
    required this.videoId,
  });

  /// The ID of the video to view
  final int videoId;
}
