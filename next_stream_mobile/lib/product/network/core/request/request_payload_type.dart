/// RequestPayloadType enum defines the type of the payload. Json, form data,
/// etc.
enum RequestPayloadType {
  /// The payload is in json format.
  json,

  /// The payload is in form data format.
  formData,
}
